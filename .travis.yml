language: node_js
node_js:
  - "10"
cache:
  directories:
    - node_modules
install:
  - npm install -g codecov
before_script:
  - npm install
script:
  - istanbul cover ./node_modules/mocha/bin/_mocha --reporter lcovonly -- -R spec
  - codecov
  - npm test
